export class line_class {
  startLine: string;
  endLine?: string;
  id: string;
  status: string;
  sections: line_class[];
  leaderLine?: any //??????????????????????????????
  path? : any //??????????????????????????????????

  constructor(
    startLine: string,
    id: string,
    status: string,
    sections: line_class[],
    endLine?: string,
    leaderLine?: any,
    path? : any
  ) {
    this.startLine = startLine;
    this.endLine = endLine;
    this.id = id;
    this.status = status;
    this.sections = sections;
    this.leaderLine = leaderLine;
    this.path = path;
  }
}
